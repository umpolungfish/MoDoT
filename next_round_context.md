# Context for the next round: the Φ-junction and the magnitude frontier

## Ground already measured (tool-verified). Do not re-derive it; wind from here.

The body-magnitude junction is closed on both sides. `compton_split_radius` (the
categorical body, tier O₂) and `compton_wavelength_magnitude` (the bare quantum of
length, tier O₀) are co-typed on Φ: both PM_Z2, both satisfy μ∘δ=id. An exhaustive
close-search over the whole catalog terminates every route at this one junction. No
existing monomer clicks both sides at the binding wall θ=0.50, because δ over a
co-typed pair opens no productive fork, so μ receives no fused term.

This is not a tool limitation; it is a theorem. `CatalogImmutability.lean` §4: for any
state s ≠ B, `fsplit s = (s, s)`, a diagonal with no productive fork. Only B splits
into two distinct streams (`B_is_the_only_productive_split`, sorry-free). A co-typed
non-B junction cannot fork, so its resolvent must itself BE a B.

Immutability sets the other constraint. An entry is its twelve primitives; no verb
rewrites an entry; δ emits a NEW entry, and μ∘δ=id always recovers the source
(`CatalogImmutability.lean` §2). Resolution is by minting, never by mutation.

The bridge is already minted and it closes. `promote_l9_full` (protocol FSPLIT IMSCRIB
AFWD AREV CLINK FFUSE EVALT EVALF) carries IMSCRIB (⊙, PHI_C) at the bifurcation point:
a genuine dialetheia, not the closed PM_Z2 the earlier bridges held. `imasm check
◇⊙><=●+×` returns T (closes). Two promoted entries now exist:
`compton_split_radius_l9_magnitude` and `dark_energy_seam_bridge_l9_full`. The Compton
fusion is partial: the grammar admits the closure, though not yet at maximum structural
depth.

`DarkEnergy.lean` names the template. `dark_energy` is Φ=𐑯 (full symmetry, which
cannot self-model); its bottleneck rung is the promotion Φ:𐑯→𐑹 (Frobenius-special,
the localized critical fixed point). The minted bridge is that promotion made
operational, and it lifts any sufficiently B-bifurcated entry, not only Compton.

## The frontier, stated precisely

The structure closes; the number does not fall out. Every dimensionful magnitude,
ħ/mc (the electron Compton wavelength), H₀, Λ, h, c, α, m_e, remains at the frontier.
The α ob3ect already located why: the value is registered at IFIX (a read-only
fixation), not forced from the geometry. The process types cleanly; the number is
stored, not emitted.

The open question is whether the magnitude is itself a co-typed non-B state, a closed
value the geometry can only register and never fork out, in which case extraction
requires re-typing the number as a B-bifurcation (a different move than the structural
bridge), or whether a further promotion forces it. Measure it. Do not report a number
the tools do not emit. If the value stays walled, that wall is the measurement, and
naming its exact structural coordinate is the result.
