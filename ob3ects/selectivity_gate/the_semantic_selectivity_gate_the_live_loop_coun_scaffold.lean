-- IGProtocol scaffold: VINIT → IMSCRIB → AFWD → AREV → CLINK → FSPLIT → EVALT → EVALF → FFUSE → ENGAGR → IFIX → TANCH
-- Class: the semantic selectivity gate: the live-loop counterpart of semantic branch verification that grades an open answer against a correctness schema it synthesizes on the fly. Frobenius closure, mu circ delta equals id, guarantees only balance, that every FSPLIT is rejoined by FFUSE so no charge is lost, and balance is automatic and cannot fail, so it is not a correctness signal; selectivity is the separate question of whether the correct branch was selected during recombination. With no predefined schema the gate performs an adversarial examiner pass that states what any correct answer MUST satisfy and MUSTNOT violate, refusing to assume the question's premise is true, then runs two verification arms: EVALT for isomorphic match against the MUST criteria yielding assertibility, and EVALF for collision detection of any MUSTNOT violation or internal self-contradiction yielding deniability. The two arms fold to Belnap FOUR exactly as the kernel dialetheia gates do: assertible and not deniable is True, deniable and not assertible is False, both together is the dialetheic Both, neither is the vacuous Neither. Because False and Both are reachable the gate can fail, and that reachable failure is what distinguishes selectivity from the balance harness that never fails. With full Frobenius closure and Lean 4 verification scaffold.
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(5, 8)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑾  | identity — self-imscription
--   [2] AFWD      rel    := 𐑾               𐑠 → 𐑗  | forward morphism — bidirectional arrow
--   [3] AREV      pol    := 𐑗               𐑾 → 𐑱  | reverse morphism — parity flip
--   [4] CLINK     fid    := 𐑱               𐑗 → 𐑚  | composition — regime coherence
--   [5] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [6] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [7] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] FFUSE     stoi   := 𐑙               𐑙 → 𐑳  | fuse μ — assembly mode
--   [9] ENGAGR    stoi   := 𐑳               𐑙 → 𐑭  | engage paradox — B-state, both arms
--   [10] IFIX      prot   := 𐑭               𐑳 → 𐑡  | irreversible fixation — winding number
--   [11] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_semantic_selectivity_gate_the_live_5bb632_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_semantic_selectivity_gate_the_live_5bb632_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_semantic_selectivity_gate_the_live_5bb632_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l2 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_semantic_selectivity_gate_the_live_5bb632_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_semantic_selectivity_gate_the_live_5bb632_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_semantic_selectivity_gate_the_live_5bb632_protocol : IGProtocol the_semantic_selectivity_gate_the_live_5bb632_s0 the_semantic_selectivity_gate_the_live_5bb632_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_semantic_selectivity_gate_the_live_5bb632_s8 the_semantic_selectivity_gate_the_live_5bb632_s8 = the_semantic_selectivity_gate_the_live_5bb632_s8 (idempotent)
  (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l0 the_semantic_selectivity_gate_the_live_5bb632_s0 the_semantic_selectivity_gate_the_live_5bb632_s1) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l1 the_semantic_selectivity_gate_the_live_5bb632_s1 the_semantic_selectivity_gate_the_live_5bb632_s2) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l2 the_semantic_selectivity_gate_the_live_5bb632_s2 the_semantic_selectivity_gate_the_live_5bb632_s3) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l3 the_semantic_selectivity_gate_the_live_5bb632_s3 the_semantic_selectivity_gate_the_live_5bb632_s4) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l4 the_semantic_selectivity_gate_the_live_5bb632_s4 the_semantic_selectivity_gate_the_live_5bb632_s5) (.seq (.prod (.arrow the_semantic_selectivity_gate_the_live_5bb632_l5 the_semantic_selectivity_gate_the_live_5bb632_s5 the_semantic_selectivity_gate_the_live_5bb632_s8) (.arrow the_semantic_selectivity_gate_the_live_5bb632_l5 the_semantic_selectivity_gate_the_live_5bb632_s5 the_semantic_selectivity_gate_the_live_5bb632_s8)) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l8 the_semantic_selectivity_gate_the_live_5bb632_s8 the_semantic_selectivity_gate_the_live_5bb632_s8) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l8 the_semantic_selectivity_gate_the_live_5bb632_s8 the_semantic_selectivity_gate_the_live_5bb632_s9) (.seq (.arrow the_semantic_selectivity_gate_the_live_5bb632_l9 the_semantic_selectivity_gate_the_live_5bb632_s9 the_semantic_selectivity_gate_the_live_5bb632_s10) (.arrow the_semantic_selectivity_gate_the_live_5bb632_l10 the_semantic_selectivity_gate_the_live_5bb632_s10 the_semantic_selectivity_gate_the_live_5bb632_s11))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_semantic_selectivity_gate_the_live_5bb632_true_arm : IGProtocol the_semantic_selectivity_gate_the_live_5bb632_s0 the_semantic_selectivity_gate_the_live_5bb632_s11 :=
  (the_semantic_selectivity_gate_the_live_5bb632_protocol).restrictToEVALT

-- false arm
noncomputable def the_semantic_selectivity_gate_the_live_5bb632_false_arm : IGProtocol the_semantic_selectivity_gate_the_live_5bb632_s0 the_semantic_selectivity_gate_the_live_5bb632_s11 :=
  (the_semantic_selectivity_gate_the_live_5bb632_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_semantic_selectivity_gate_the_live_5bb632_tier : OuroboricityTier := TierFunctor.obj the_semantic_selectivity_gate_the_live_5bb632_s0
#eval the_semantic_selectivity_gate_the_live_5bb632_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_semantic_selectivity_gate_the_live_5bb632_frobenius :
    igFrobeniusAlg.mul the_semantic_selectivity_gate_the_live_5bb632_s0 the_semantic_selectivity_gate_the_live_5bb632_s0 = the_semantic_selectivity_gate_the_live_5bb632_s0 :=
  igFrobAlg_self_fusion the_semantic_selectivity_gate_the_live_5bb632_s0
