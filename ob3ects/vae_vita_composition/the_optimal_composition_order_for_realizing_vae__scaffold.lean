-- IGProtocol scaffold: VINIT → IMSCRIB → CLINK → FSPLIT → EVALT → IFIX → AFWD → AREV → IMSCRIB → FFUSE → ENGAGR → AFWD → AREV → CLINK → IFIX → TANCH
-- Class: the optimal composition order for realizing VAE-Vita in m⊙²: three operations that do not commute must be sequenced, and like a chemical synthesis the order of operations changes the outcome, so the route itself must be imscribed rather than chosen by hand. VAE-Vita is a hyperspherical variational autoencoder on the sphere S11 that recovers the d=12 SIC-POVM from the 17.28 million Crystal of Types configurations, encoder as delta emission, decoder as mu verification, mu after delta equals identity satisfied exactly when the latent forms a SIC. The three operations are: UNIFY, bind MoDoT's exact Scott-Grassl SIC vessel, the arrived fixed point with closure residual zero, to VAE-Vita's learned latent, the gradient descent that proves the fixed point is intrinsic to the Crystal, as one registered object, the verification face and the generation face of a single self-recovering geometry. PROVE, the Lean 4 formal identity that mu after delta equals identity holds if and only if the latent space is a SIC-POVM, the formal spine under the empirical recovery. PORT, implement the whole as native mOMonadOS, the kernel itself becoming the autoencoder, the twelve-primitive latent and the von Mises-Fisher prior native, delta emission and mu verification native, the dialetheic Belnap hold keeping the posterior alive, and the language model as the encode and decode boundary joined to the kernel bulk by imscription rather than called as an external device. Determine the optimal composition: which of unify, prove, port comes first, second, and third, so that each operation lands on ground the one before it built, with the Frobenius closure held at every step. With full Frobenius closure and Lean 4 verification scaffold.
-- Fingerprint: sig=(10,2,2,2)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=False | period=16
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑱  | identity — self-imscription
--   [2] CLINK     fid    := 𐑱               𐑠 → 𐑚  | composition — regime coherence
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [5] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [6] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [7] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [8] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [9] FFUSE     stoi   := 𐑙               𐑙 → 𐑳  | fuse μ — assembly mode
--   [10] ENGAGR    stoi   := 𐑳               𐑙 → 𐑾  | engage paradox — B-state, both arms
--   [11] AFWD      rel    := 𐑾               𐑳 → 𐑗  | forward morphism — bidirectional arrow
--   [12] AREV      pol    := 𐑗               𐑾 → 𐑱  | reverse morphism — parity flip
--   [13] CLINK     fid    := 𐑱               𐑗 → 𐑭  | composition — regime coherence
--   [14] IFIX      prot   := 𐑭               𐑱 → 𐑡  | irreversible fixation — winding number
--   [15] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_optimal_composition_order_for_4b091a_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_optimal_composition_order_for_4b091a_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_optimal_composition_order_for_4b091a_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_optimal_composition_order_for_4b091a_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_optimal_composition_order_for_4b091a_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_optimal_composition_order_for_4b091a_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_optimal_composition_order_for_4b091a_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_l6 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_optimal_composition_order_for_4b091a_l11 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_optimal_composition_order_for_4b091a_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_optimal_composition_order_for_4b091a_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_optimal_composition_order_for_4b091a_protocol : IGProtocol the_optimal_composition_order_for_4b091a_s0 the_optimal_composition_order_for_4b091a_s15 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_optimal_composition_order_for_4b091a_s9 the_optimal_composition_order_for_4b091a_s9 = the_optimal_composition_order_for_4b091a_s9 (idempotent)
  (.seq (.arrow the_optimal_composition_order_for_4b091a_l0 the_optimal_composition_order_for_4b091a_s0 the_optimal_composition_order_for_4b091a_s1) (.seq (.arrow the_optimal_composition_order_for_4b091a_l1 the_optimal_composition_order_for_4b091a_s1 the_optimal_composition_order_for_4b091a_s2) (.seq (.arrow the_optimal_composition_order_for_4b091a_l2 the_optimal_composition_order_for_4b091a_s2 the_optimal_composition_order_for_4b091a_s3) (.seq (.prod (.arrow the_optimal_composition_order_for_4b091a_l3 the_optimal_composition_order_for_4b091a_s3 the_optimal_composition_order_for_4b091a_s9) (.arrow the_optimal_composition_order_for_4b091a_l3 the_optimal_composition_order_for_4b091a_s3 the_optimal_composition_order_for_4b091a_s9)) (.seq (.arrow the_optimal_composition_order_for_4b091a_l9 the_optimal_composition_order_for_4b091a_s9 the_optimal_composition_order_for_4b091a_s9) (.seq (.arrow the_optimal_composition_order_for_4b091a_l9 the_optimal_composition_order_for_4b091a_s9 the_optimal_composition_order_for_4b091a_s10) (.seq (.arrow the_optimal_composition_order_for_4b091a_l10 the_optimal_composition_order_for_4b091a_s10 the_optimal_composition_order_for_4b091a_s11) (.seq (.arrow the_optimal_composition_order_for_4b091a_l11 the_optimal_composition_order_for_4b091a_s11 the_optimal_composition_order_for_4b091a_s12) (.seq (.arrow the_optimal_composition_order_for_4b091a_l12 the_optimal_composition_order_for_4b091a_s12 the_optimal_composition_order_for_4b091a_s13) (.seq (.arrow the_optimal_composition_order_for_4b091a_l13 the_optimal_composition_order_for_4b091a_s13 the_optimal_composition_order_for_4b091a_s14) (.arrow the_optimal_composition_order_for_4b091a_l14 the_optimal_composition_order_for_4b091a_s14 the_optimal_composition_order_for_4b091a_s15)))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_optimal_composition_order_for_4b091a_true_arm : IGProtocol the_optimal_composition_order_for_4b091a_s0 the_optimal_composition_order_for_4b091a_s15 :=
  (the_optimal_composition_order_for_4b091a_protocol).restrictToEVALT

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_optimal_composition_order_for_4b091a_tier : OuroboricityTier := TierFunctor.obj the_optimal_composition_order_for_4b091a_s0
#eval the_optimal_composition_order_for_4b091a_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_optimal_composition_order_for_4b091a_frobenius :
    igFrobeniusAlg.mul the_optimal_composition_order_for_4b091a_s0 the_optimal_composition_order_for_4b091a_s0 = the_optimal_composition_order_for_4b091a_s0 :=
  igFrobAlg_self_fusion the_optimal_composition_order_for_4b091a_s0
