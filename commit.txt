I restore the shared core required by the native GPU build

I integrated the compatible imasm_core source from f06eb03, including lattice
flow, ring specifications, current glyphs, and engagr. I extracted ask's graph
flow evaluator into the shared core so ask and the GPU controls use the same
gate semantics. I updated the stale example test to use the current ≻ glyph.
