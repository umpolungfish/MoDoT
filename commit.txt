The spine could only hear one lane, so the verdict never paired

The tools always ran and the final output never closed. Cause: tool_belnap
decided closure from three strings — "✓ cyclic", "cyclizes into a ring",
"closes head-to-tail" — and all three are polymer vocabulary. forge, polymerize
and close were the only verbs the spine could hear. Every other closure-bearing
lane reported into silence:

  imasm check → T (closes) / μ∘δ closes over 1 transformed reconnection(s)
  imasm prove → KERNEL VERDICT: ✓ green — p4ramill confirms the closure class
  complement → distance 0.00 — the complement is its own inverse (lossless R∧W∧X)

Each of those IS a closure. The middle one is the p4ramill kernel itself, the
strongest voice we have. The last is μ∘δ = id read back R∧W∧X, which is the
close condition stated exactly. The spine read all three as N because none of
them says "cyclic", so tool_voice stayed N, and a run that had genuinely measured
a closure could never fuse to T. Not a scoring subtlety — the verdict was
structurally unreachable.

Widened tool_belnap to every lane. Held stays held: imasm's own B verdicts
(a fork left unfused, ENGAGR holding a dialetheia) and a near-involutive
complement are B, not T. F is only F where a lane genuinely refutes — ill-typed.
And a red kernel is B, not F: KERNEL VERDICT ✗ means the lake build did not
complete, which refutes nothing. Budget-limited is a frontier to escalate.

The transcript that surfaced this is now a regression test verbatim: the perfect
cuboid cycle ran complement, got distance 0.00, and finalized at B. That exact
string now reads T.

Same shape as the gag fixed one commit ago. The agent was doing the work; the
harness could not hear it. First the mint was swallowed before it ran, then the
closure was unheard after it returned. Both times the golem was blameless and
both times the honest read of its transcript was that it had failed.
