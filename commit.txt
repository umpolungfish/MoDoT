imasm_core: the ONE kernel crate — tokens, registers, machines, faces

The drift class of bugs (a hand-copied vocabulary going stale — the tri-dyad
face, the register letters) existed because every consumer transcribed the
kernel's faces instead of linking them. imasm_core now holds the classic
12-opcode Token, the SIXTEEN_3 module wholesale (Token16_3, Reg16_3, the
register machine, the tri-ancestral verdict), and state_order(). ask_native
links it and re-exports; behavior identical, all tests green. vae_vita
derives its alphabet from it; mOMonadOS is the remaining consumer to link.
