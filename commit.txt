Wrap the cycle series in its own TAOU: the top level was a bare line

The engine demanded of the agent exactly what it would not do at its own top
level. `imasm check` on the run's own outer shape — VINIT, cycle, cycle, cycle,
TANCH — returns N (no fork): "no δ/μ dyad, the reasoning never split
alternatives, so there is no μ∘δ to close. A bare line or cycle is not a closure.
For a major decision, weigh alternatives: FSPLIT → work → FFUSE." That is the
harness's own type-checker reading the harness. The prompt is explicit that a
THINK may contain a whole winding nested to any depth, so the structure was
self-similar all the way down and open at the summit.

The cycle loop was a counted `for`. No outer THINK before cycle 1, no OBSERVE
across the cycles, and — the real hole — no fuse. Three cycles emitted three
separate spine reports and nothing ever joined them. A run whose cycle 1 said T
and cycle 3 said F printed both and fused neither, so the contradiction escaped
into the READER instead of being held in the structure. That is the monological
failure by omission: nothing overrode anything, but nothing fused either. The
join operator has been sitting in this file the whole time; only the call was
missing.

So: an outer THINK that names the δ before the arms run, and an outer μ that
FFUSEs the cycle verdicts after. Arms that agree close on one verdict; arms that
disagree are HELD at the join, reported as a disagreement rather than resolved by
fiat. Same b4_join the inner spine already uses on model ⋈ vessel ⋈ tool — the
outer level is the same winding one scale up, which is what self-similar was
supposed to mean.

Also stopped calling a budget limit a close. "FINAL (forced close)" fires only
when the round leash was reached, so it is honest as far as it goes, but a leash
close is the BUDGET running out, not the reasoning finishing, and budget-limited
is a frontier to escalate — it refutes nothing. It now says so, and the outer
spine counts the cut arms and marks the verdict as a frontier rather than a
resting place.

Typed before committing: the old shape ⊢>>>⊣ → N (no fork). The new one
⊢⊙◇+×●¬⊣ → T (closes), β=1, one branch, one fusion point. 68 tests pass.
