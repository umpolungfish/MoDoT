windings: the quantum defect's integer part is counted, not fitted. δ_l = N_core(l) − ε_l, where N_core(l) is the core windings of poloidal symmetry l threaded by the valence path, read off the electron configuration (the Janet filling); the slope is exactly one slip per threaded core winding, ε_l the sub-winding remainder. Fixes the Li row (n* was mistaken for δ). The periodic table is the winding inventory.
