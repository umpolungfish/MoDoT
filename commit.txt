State the chem↔math isomorphism both ways in the final output

The reports named operations in one register only — "excitation",
"cyclization", "forge" as chemistry — while the mathematical face went
unspoken, so the isomorphism read one direction. It runs both ways: the
engine performs one act with two lossless faces (R∧W∧X), and the output
should say what each operation MEANS chemically AND mathematically.

Added verb_isomorphism(verb) -> (chemical, mathematical): a deterministic
key covering click / excite / cycle / polymerize·close / forge·material·
arrange / set / distill / crystallize / scan / homolyze·cleave. After the
agentic loop closes, an ── ISOMORPHISM ── block prints both faces for
exactly the verbs that actually ran (grounded like everything else —
executed_verbs only). The final-synthesis prompt also asks the model to
state both readings inline for any operation it leans on.

Examples: excite = raising Criticality ⊙ to the exceptional-point
resonance (chem: promotion to the excited state); cyclize = a composite
returning to its start, ρ=2 exactly (chem: head-to-tail macrocyclization).

New test pins both faces present and distinct for the key verbs; suite
green (27 passed).
