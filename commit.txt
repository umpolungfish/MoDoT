The tri-dyad wears its true face: FSPLIT3/FFUSE3 are ∈/∋ everywhere

Per the unified guide the correct tokens are ∈ and ∋; ☊/☋ were a drifted
face. glyph() corrected, help text and ref examples corrected, from_glyph
keeps the drifted pair as parse-compat so old words still load. four_glyph
collapses to glyph(): B of FOUR IS the state {T,F} and wears ▀; the ⊤
artifact is gone; no Latin token anywhere, verdicts included.
