Wire the kernel closure certificate into click-maths — "closes" is now verified, not asserted

The standing open hook is closed. `./ask --click A B --certify` takes the fused
product tuple, renders it as a real Lean Imscription (glyph->constructor from the
canonical imscribing_grammar/scripts/gen_clay_canonical_tuples.py table), and
builds `igFrobeniusAlg.mul p p = p := igFrobAlg_self_fusion p` as the prover's
scratch module through `lake build`. Green = the kernel's own verdict that the
product Frobenius-closes; the scratch is restored to placeholder after. Reuses
prover.rs's compile_lean + restore_placeholder (made pub(crate)) rather than
duplicating the lake machinery.

Verified live: boundary_operator x winding_number -> product
<𐑦𐑸𐑾𐑿𐑐𐑪𐑲𐑠⊙𐑫𐑳𐑭> -> KERNEL-CERTIFIED green.

Honest about what it certifies, because igFrobAlg_self_fusion is the UNIVERSAL
diagonal Frobenius theorem (holds for every valid Imscription): the certificate
confirms the product is a well-formed kernel Imscription that mu-delta-closes --
the BALANCE ledger, kernel-verified -- not the fusion's selectivity (that's the
click's complementarity/orthogonality, done in Rust) and not axiom-conformance
(the cross-primitive axioms are assumed, not enforced at construction). That is
exactly ALKAHEST section 11's balance-vs-selectivity: the click logic is the
selectivity, the kernel certificate is the balance. Both gated now -- the balance
by the real kernel instead of my assertion, which was the whole point of the hook.
