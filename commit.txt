I taught the excriber the fan-out and made its diversity mechanical.

The prompt now carries ONE worked example word with THREE wildly different
excriptions of it (a jury trial, a reaction fork, a bird between branches for
the classic face; triage, a prism, an estate dispute for the trilattice):
three domains, one structure, teaching the shape of excription rather than an
answer. The live 1.7B promptly parroted the example excriptions verbatim at
any temperature, so the diversity is enforced mechanically, not rhetorically:
the guessing domain is ASSIGNED per candidate, rotated by the word over twenty
domains, and a guess whose word-set overlaps a spent example or an
already-taken guess is refused with one hotter retry in the next domain over,
else the candidate fails honestly as parroted. Guide Part XIV updated.

Verified: build green under local,cuda; 5/5 learn tests; live tri run produced
four candidates in four domains (cartography, chess, archaeology,
fermentation) with zero parroting, guesses recorded, trajectory appended.
