Wire the catalyst mode into click-maths — from the ob3ect the Grammar designed

When isomorphism wouldn't click as a reagent, we didn't guess the fix -- we made
an ob3ect. Described the desired thing neutrally (a lossless, self-restoring
operation that lowers the threshold for two fragments to fuse and returns to
itself unchanged) and let auto.py design it. The Grammar came back with the
Michaelis-Menten cycle, exactly: IMSCRIB defines the invariant catalyst, AFWD
lowers the barrier, CLINK couples catalyst to the pair, EVALT fires the fusion
at the lowered threshold, AREV restores the barrier, IMSCRIB verifies the
catalyst returned unchanged (mu-delta=id, regenerated, not consumed). phase_6:
"one fused product and one INDEPENDENT catalyst." And it respected the
DeltaG-double-dagger-not-DeltaG guardrail on its own -- the catalyst moves the
barrier (AFWD/AREV), never the complementarity.

Ported that protocol into ask_native/src/click.rs as click_pair_catalyzed and
./ask --click A B --catalyst C. The catalyst's strength is its Frobenius-special
character, discriminated by Parity Phi (Phi=roar is the mu-delta=id map). Strength
scales the barrier reduction: theta_eff = theta * (1 - strength). isomorphism
(Phi=roar, strength 1.0) drops theta to 0.

This resolves the isomorphism question the right way: it's not a broken click
substrate, it's the CATALYST -- the mu itself. Verified live:
- adjoint_pair x hopf_antipode: opposite on T<->H but Delta=0.42 < theta=0.5, so
  bare it REFUSES. Catalyzed by isomorphism -> theta 0.50->0.00 -> CLICK. The
  fragment that couldn't react is now what enables the reaction.
- GUARDRAIL: isomorphism x winding_number (same-sign) catalyzed -> barrier
  dropped to 0.00 and it STILL refuses. A catalyst speeds an allowed fusion; it
  cannot drive a forbidden one. The law demonstrated at the limit.

Cleaned the dead-code warnings (CONJUGATE_PAIRS marked reserved for the future
addition mode on the pinned pairs; click_pair_catalyzed now the real entry point).
