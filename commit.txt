harness: stop the error-echo dyad burn, join evidence to windings, absorb name drift

Three generator fixes from the reseed run's transcripts, plus the next brief.

The mu-face scanner dispatched a quoted error line as a call: `lean: no such
file '<path.lean>'` re-parsed with the colon trimmed off the verb, feeding the
error's own words back as arguments, twice. A `verb:` token with the colon
attached is output being quoted, never a call; and the mu face now refuses
manual placeholders (<path.lean>) exactly as the delta face does.

tool_calls.jsonl now records the WINDING each call belongs to. The prior
audit had to reconstruct attribution by timestamp interleaving; the join is
now written down at the moment it is known.

Catalog lookups resolve through one shared find_entry: exact match first,
then normalized (case-insensitive, -/_ equivalent). The drift class that
burned the monomer-not-found dyads now resolves; an AMBIGUOUS normalized
match resolves to nothing rather than to a guess.

The lean-verb test pinned a live library file that used to be red as its
broken fixture; the file got repaired and the test started asserting a stale
wet lab. It now writes its own broken fixture and asserts on the carried
identifier, not one kernel version's phrasing. 102 tests green.

questions/ring_turnover.txt is the next submission: consolidate the doubled
ring entries, run the catalytic turnover (second Omega delivery or measured
saturation), and test whether the ring's containment F-breach and its
unpaired mode are one fact in two registers. Every negative in the
discipline block is paired with its door.
