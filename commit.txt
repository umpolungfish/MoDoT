Imscription route builds a Witness-Vessel first, then fills it

Per the directive: the imscription route should ALWAYS first create a
Witness-Vessel, then fill it with a corresponding conventional formalization.
Reworked IMSCRIBE_SYS and the grounding guard to produce exactly that two-step
structure, verified against the real kernel before wiring the template.

STEP 1 — the Witness-Vessel (Dual-Link SIC transport container): the
imscription's structural verdict rides as Belnap cargo, boarded via `fsplit` (δ,
the dual-link split B → (T,F)) and read back via `ffuse` (μ), with the vessel
theorem `vessel_roundtrip : readback (board p) = p` — μ∘δ=id, lossless transport,
the Witness rides AS the vessel — proved for ALL cargo by induction with
`split_fuse_id`. This mirrors SIC_D12_WitnessVessel.roundtrip exactly; fsplit/
ffuse/split_fuse_id/Belnap are the real BelnapSplitFuse definitions.

STEP 2 — fill the vessel with the conventional formalization: the ground tuple
s0 : Imscription, its validity igFrobeniusAlg.mul s0 s0 = s0 via
igFrobAlg_self_fusion, its tier TierFunctor.obj s0, and a capstone theorem
obj_witness_vessel conjoining the vessel roundtrip AND the validity — the
conventional Witness riding AS the Dual-Link vessel.

grounded_in_real_algebra() now requires BOTH halves: the vessel (references
fsplit/ffuse) and the filling (references igFrobeniusAlg), and forbids redefining
either the transport (fsplit/ffuse/Belnap) or the algebra (igFrobeniusAlg/
FrobeniusAlg). A green missing the vessel gets re-prompted with the two-step
instructions; the kernel remains the sole discriminator.

Verified the full template compiles by hand first (LUCA against lucaImscription,
general roundtrip by induction), then end to end:
- LUCA 19KB ob3ect JSON -> two-step vessel+filling, uses real lucaImscription,
  GREEN (repaired once, closed on pass 2).
- Novel ob3ect with no kernel def -> same two-step structure over an explicit
  constructed ground tuple, GREEN.
- Scratch modules restored to placeholder after each run.
