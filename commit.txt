harness: catalog-writing verbs flush the tool-result cache.

The five_constant_ring trap, caught live: a round emitted containment_boundary
BEFORE register in the same batch, the miss ("Unknown system") was cached under
its verb+args signature, and the agent's honest next-round re-run would have
received the stale miss labeled (cached) — concluding the registered assembly
cannot be typed, when the truth is the cache predated the write.

Fix: when register or imscribe executes (the catalog writers), ran_results is
cleared and the agent is told in the same TOOL RESULTS block that the cache
was flushed and re-emitted calls will re-execute against the new state.
Reads are only cacheable while the state they read is unchanged; a write
sections the cache the same way it sections the catalog.

All 117 tests pass.
