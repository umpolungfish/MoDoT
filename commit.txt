imasm_core: the close condition moves into the portable kernel

Graph, ancestry pairing, ClosureState, and the whole gate (word_verdict)
extracted from ask_native/src/imasm.rs into imasm_core/src/check.rs so the
verdict can be spoken wherever the kernel runs — no_std + alloc, no host.
ask_native keeps presentation, spectral analysis, and the tool registry as
a GraphExt extension over the core Graph. tri_ancestral_verdict made pub
for the same reason. 111 tests green, `imasm check` verdicts unchanged.
