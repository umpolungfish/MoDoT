Serve the word instruments on every imasm graph verb, and on write

chain, ring, protocol, star, comb, bubble, wire, classify and the rest all
funnel through one report() function that already prints the graph's own
close condition (does this wiring's FSPLIT reach its FFUSE). That is a
different question from what weight/banked/cycle/insert ask of the same
code string read as a plain line, so report() now runs all four and labels
them separately, right under the existing graph verdict, instead of leaving
the reader to copy the code string into another command by hand.

imasm write (the twelve types composed into their word) gets the same
treatment on the word it just produced.

Left imasm derive and imasm cycle alone: those are a different pair of
verbs in this file, the type<->word round trip and the catalog-wide
consistency sweep, not new words being handed back for someone to act on.
