--scan-mediators honours --excite, instead of asking for it

The flag never reached the scan. run_scan_mediators took only the two names and
the top count, so it compared GROUND-STATE ⊙, found the pair thermoneutral, and
printed "Excite one first" — to a caller who had written --excite on the same
line. The one instruction it gave was the one already followed.

It now takes the photo flag and applies the same excite_tuple promotion --set
applies, before the comparison. On sk_kp1/pk_kp1 the gap opens (⊙ 𐑢→𐑻) and the
scan runs: 4746 holdable candidates over 8425 entries.
