Bring the README up to date with natures and the IG tool corpus

The README predated two additions, so it now documents them. Directory
structure lists the modules that were missing (prover, router, natures,
ig_tools) and ob3ects/primitives/. Two new sections:

- IG tool corpus: the 43 IG_inquiry catalog verbs, dispatched natively
  from the Rust loop via run_ig_tool, with the verb table grouped by
  kind and the modot.ig_tools API / de-shadow note.
- Primitive-type natures: the 49 kernel constructors loaded by
  natures.py, the Nature fields, the tiles_crystal proof, cotype and the
  paradice, and the pointer to the kernel PrimitiveType files.

Requirements gains the imscribing_grammar corpus dependency (IG_ROOT).
Date rolled to 2026-07-13. Verified: every IG verb named in the new
table is a real dispatcher verb and the natures API resolves.
