lattice_cycle and weight_flow reach MoDoT.

The bridge already loads the live IG dispatcher, so both arrive by naming their
arguments rather than by reimplementing anything.

lattice_cycle walks a word around its ROTAT orbit: the verdict and the topology
hold across it, the final register does not, so the phase is the only handle on
where a word comes to rest. weight_flow counts what the union throws away, the
fork being a set and the fuse a union.
