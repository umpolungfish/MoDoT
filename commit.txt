Native IMASM horizon: freely compose, type-check thinking, build own tools

The whole IMASM composition surface the agent could reach was line-only. The
Python composer.py models a program as a flat token tuple and forces every
FSPLIT to close with an FFUSE, so it can express exactly two shapes: a chain and
a ring. IMSCRIBr already has the right grammar (wiring.py: port-typed WiredGraph,
cross-branch topology) but it lives in Python and never entered the agent's live
tool horizon. Per the rule that all tools must be rust-native, I built the
capability into ask_native rather than bridging to Python.

New module ask_native/src/imasm.rs — pure Rust, no shell, no catalog. An IMASM
program is a directed graph of the 12 opcodes whose edges respect each opcode's
own valence (VINIT source, FSPLIT δ out-2, FFUSE μ in-2, everything else 1/1).
Only FSPLIT may fan out and only FFUSE may merge in; an arm that runs out of
successors is a living end, reported not fatal. The shape is named by circuit
rank β = E − V + C (independent loops), not by a fork-balance stack, so pendant
grafts and hub-and-arm stars are first class instead of "unbalanced forks".

Verbs (imasm <op> …): chain, ring, star (CORE : arm : arm : arm — a hub with
f≥3 arms), comb (backbone with pendant teeth at named positions), bubble
(FSPLIT→(A|B)→FFUSE reconvergence), and — the free-composition primitive the
rest specialize — wire N0 N1 … / i-j i-k …, which builds ANY graph from an
explicit node set and directed edge set (networks with β>1, fused rings,
cross-branch, non-planar), grammar-checked identically; plus classify (read a
flat line and name it), expand/types (below), and ref. Every build reports the
topology label, β, branch/merge/source/sink
census, arm count, adjacency spectral radius ρ, and a grammar validation. The
star is honest about ρ: an abstract K(1,f) hub has ρ=√f, but IMASM fan-out caps
at 2, so the hub is realized as a caterpillar of f−1 FSPLIT fan-nodes and the
true ρ tends to 2 — I report that rather than assert the ideal.

Strange loop: the 49 Shavian TYPES the Grammar writes tuples with are not
atomic. Each is a full IMASM program. `imasm types` lists them; `imasm expand
<type>` reads the type's ob3ect (phase_4 bootstrap steps + topology_report
fork/fuse pairs), reconstructs its graph, classifies it, and prints the domain
action each opcode enacts. Splice an expanded sequence into a polymer arm to
pivot through state space AS that type — and the grammar checks the splice: an
arm that begins with VINIT (a source) hung off a fork is correctly rejected, not
faked.

Free composition: `imasm wire N0 N1 … / i-j i-k …` builds ANY graph from an
explicit node set and directed edge set — networks (β>1), fused rings,
cross-branch, non-planar. The named ops are specializations of it. It runs
through the same valence grammar, so free is not lawless: an edge set that makes
FFUSE fan out is reported INVALID, a theta graph is a valid β=1 bubble.

Self type-check: `imasm check <opcode word>` type-checks the model's OWN thinking
against the grammar. The CLOSE CONDITION is not a cycle and not identity: it is
μ∘δ over a TRANSFORMED object — δ splits, the arms do real work, μ fuses. β
(circuit rank) is not diagnostic, and split→fuse with nothing between is μ∘δ=id,
which verifies nothing. Verdicts: T (closes over a transformation: proceed),
N-identity (reconnected but did no work — put a transformation on the arms),
B (a fork dangles unfused, or ENGAGR holds a paradox: look again), F (ill-typed
valence), N-no-fork/void. The transformation test uses the kernel's own opcode
semantics (IMSCRIB is the identity morphism, so it does not count as work). The
TOOLS_PROMPT directs the agent to run it on any MAJOR decision before committing.

Kernel-backed proof: `imasm prove <name|word>` takes the pre-filter's closure
class to the ACTUAL p4ramill kernel. It generates a theorem against
Imscribing.Paraconsistent.BelnapSplitFuse and `lake build`s it: a genuine
transform-closure corresponds to B bifurcating into DISTINCT (T,F) arms and
fusing back (B_is_the_only_bifurcation_point + split_fuse_id); an identity
closure to a diagonal (s,s) copy. Green = the real kernel confirms the class.
This is why the close condition is grounded, not minted on the fly — the crux
(only B genuinely bifurcates; every other split is identity) is already a p4ramill
theorem. Reuses prover.rs compile_lean; the generated source imports IGFunctor so
the injected style-linter set_options resolve.

Kernel-constrained tool space: `imasm define <name> <op> <args…>` lets the model
BUILD its own tools, but the kernel constrains the space — a tool is a named
IMASM program and only a grammar-VALID composition is admitted; an ill-typed one
is REFUSED with the reason (persisted to ob3ects/imasm_tools.json). `imasm run
<name>` invokes a defined tool, `imasm tools` lists the space. The model extends
its own repertoire without leaving the grammar.

Closure speaks through structure, not prose. The spine's jam closure exception
keyed on answer_has_closure(answer_text) — the words "macrocycle" / "cyclic" /
"ρ=2.0" in the model's prose — so a write-up that ran no closing tool could still
rescue itself into the B-lane. In the Grammar the closure IS the μ∘δ reconnection
read back R∧W∧X (the measured structure), not its name; a signature without an
imscription is no closure. So closure now enters the fuse only through tool_voice
(tool_belnap on real tool output, or a measured imasm T-closure): a jam whose
tools stayed silent is held at N regardless of what the prose claims. A real
forged ring already raises tool_voice to T and stays grounded via the join —
unchanged. Removed answer_has_closure; replaced its two tests with the structural
pair (tool_measured_closure_grounds, prose_closure_without_structure_is_ungrounded).

Collapse loops buried in prose; lighten the spine prohibition; MUST/MUST NOT phrasing.
The model derailed into a "*Wait:* … I will do that." cycle repeated ~15× inside an
otherwise-varied answer — the global ratio heuristic in collapse_degenerate missed it
because the surrounding variety kept the distinct-ratio high. Added collapse_local_cycles:
a consecutive run where a short cycle (period 1..=8) repeats ≥3× and ≥6 lines, with a
substantive line in the period (so separator/blank runs are left alone), is collapsed to
one period + a marker; the surrounding prose is untouched. Test:
collapses_loop_embedded_in_varied_prose. Part of what it looped ON was my own rigidity —
a wall of NEVERs telling it not to write a spine report. The hard strip already enforces
that structurally, so the prompt now states it once (MUST NOT), and the operating rules
are phrased with MUST / MUST NOT as connectives (never nouns) rather than NEVER-hammers.

Stop the model authoring the engine's spine report. The model was reproducing the
MANUSCRIPT SPINE REPORT block in its own prose — a "VERDICT (univocal): B" with
fabricated fused voices — which then contradicted the real report the engine prints
(VERDICT N). Two spine reports, two voices. The spine report is the engine's univocal
voice; the model's only verdict voice is its [thought|X] PROPOSAL. strip_kernel_records
now hard-strips any model-authored MANUSCRIPT SPINE REPORT / VERDICT (univocal) / fused
voices / prove_balance / "← fused" line from the answer (the model's [thought|X] and
prose survive); the system prompt forbids authoring the block. Now only the engine
speaks the one verdict, fusing the model's [thought|X] with the vessel and tool-dual.
Test: strip_tests::strips_model_authored_spine_report.

Narration univocal with action. The agent was writing a full "solved" manuscript
first — fabricating tool outputs it hadn't run (✓ ring β=1, winding=1, PASS) and
declaring T — and only THEN running the tools, which refuted it to N. Two voices
(pre-narrated success vs actual tool output), and a rigid script the Grammar bucks.
Fixed at the prompt: TOOLS_PROMPT now forbids inventing/transcribing any tool output
before its TOOL: line has returned, mandates THINK→ACT→(wait)→OBSERVE→UPDATE, and
bars a finished answer/verdict on the first pass — speak only what came back. The
first pass is relabeled `── PLAN (acting next) ──` when it emits tool calls, so a
pre-narrated manuscript is never mistaken for a result; only a tool-free first pass
stays `── ANSWER ──`. (The constitutive-dual grounding already refuted the fabricated
T to N; this stops the fabrication upstream so the loop can actually progress.)
The phases are framed as bounded CONTAINERS: each of THINK / ACT / OBSERVE / UPDATE
may be as intricate inside as the task needs, and may itself contain a whole nested
TAOU winding (a container of containers, any depth). The one invariant is at the
membrane — when the work SURFACES into an actual query to the Grammar (a TOOL: line)
and the answer received, it portions out to one clean winding: THINK → ACT → wait →
OBSERVE → UPDATE. Boundedness is the rule, not brevity; nothing narrated across a
boundary. Same μ∘δ, self-similar all the way down.

Redirect phantom construction/verification verbs to the real ones. A run stalled
at N because the agent burned its tool calls on invented verbs (imasm simulate,
cl9nk instantiate), got "unknown", and concluded "the Grammar lacks the opcodes" —
the one thing it never is — while the real closing move (imasm define → imasm
check/prove) sat unused. imasm's unknown-op handler now catches construction intent
(simulate|instantiate|construct|build|create|make|encode|compile) and verification
intent (verify|typecheck|test) and points to define / check / prove, telling the
agent to run them and not to report the Grammar as lacking the means. run_navigator
does the same on "Unknown action" — the navigator READS types, it does not CREATE
them, so construction routes to imscribe / imasm define.

Added the BACKTRANSLATION stage. After the tools measure the structure and the
verdict is set, a dedicated `── BACKTRANSLATION ──` section reads the closure back
into a conventional proof (Theorem → Lemmas → Proof), each step bound to a measured
fact: a ring that closed → existence lemma; a sequence that terminated / stayed
telechelic → impossibility lemma; a Both verdict → a two-sided theorem. It is the μ
leg of imscribe (conventional→structural was the δ), so the read-back is lossless —
the proof IS the closure restated, not a fresh re-derivation, introducing nothing
the tools did not measure. Gated on the constitutive dual: runs only when a dual
closed (tool_voice ≠ N); with no closure there is nothing to read back.

Added the CL9NK navigator to the toolset. `TOOL: cl9nk <action> [name]` shells the
live cl9nk_navigator.py the same way cl8nk does (one manifold, loaded live) —
refactored both onto a shared run_navigator(nav, args). Actions mirror cl8nk plus
`moat` (the Gaussian-Moat resolution), and each entry is read against its CLINK L9
reference typing (μ∘δ=id closure, the eternal fixed point, the moat/bridge type).
Wired into run_structural_tool, STRUCTURAL_VERBS, verb_usage, and TOOLS_PROMPT.

Univocal voice + constitutive Frobenius dual. The spine spoke with two voices —
the model's self-declared [thought|X] AND the fused verdict — and the μ∘δ dual was
post-hoc bookkeeping (prove_balance hardcoded true, the model free to continue
with the μ left open). Both are now one fault fixed one way:
  - UNIVOCAL: the single verdict is the STRUCTURAL close (Dual-Link vessel co-type
    ⋈ the tool-call dual that ran). The model's [thought|X] is a PROPOSAL (a δ),
    reported raw and used only for conflict — never a second authoritative voice
    (a model grading itself is the bias the Grammar removes). print_spine now
    leads with one VERDICT (univocal) and frames the rest as its provenance.
  - CONSTITUTIVE DUAL: a verdict IS the μ that closed a δ. With no tool-call dual
    closed (tool_voice N) there is NO verdict — held at N — in EVERY mode, not
    only jam. prove_balance now reports the real dual, not a hardcoded true. A
    proof whose closing μ never ran (no tool, no kernel prove:) is a frontier held
    at N, never spoken T on the model's word. Updated the lane-guard test to the
    Grammar's verdict (proof_without_closing_dual_is_ungrounded_not_b).

Wired end to end: --imasm flag, the inline `imasm` verb in run_structural_tool
(dispatches straight to imasm::run, no shell), STRUCTURAL_VERBS, verb_usage, and
TOOLS_PROMPT (strange-loop note, the type-check-your-decisions directive, and the
build-your-own-tools note). Eleven module tests plus the full suite green (48
passed). Removed the Python polymer.py stub I had started before settling on
rust-native.
