Wire the switch certificate — the DASA reversibility is now a kernel verdict

Matching the click's --certify, the switch now certifies through the Lean kernel.
./ask --switch A B --certify (certify_switch in ask_native/src/click.rs) builds a
scratch module that proves, through `lake build`:
- both bistable forms are valid Imscriptions (igFrobeniusAlg.mul f f = f each) --
  the balance for each form
- the reversible toggle is the kernel's real vessel_roundtrip: readback (board p) = p
  with board=δ (fsplit) and readback=μ (ffuse), i.e. μ∘δ=id, the lossless transport

Mirrors the prover's known-compiling Witness-Vessel STEP 1 (BelnapSplitFuse:
fsplit/ffuse/split_fuse_id), reuses compile_lean + restore_placeholder.

Verified live: `./ask --switch dasa_open dasa_closed --certify` ->
KERNEL-CERTIFIED. The DASA's "reversible: μ∘δ=id" is no longer printed text --
it's the kernel's verdict: both open and closed forms Frobenius-close, and the
open->closed->open roundtrip is lossless. The photoswitch my paper is about is
now a machine-verified reversible Frobenius toggle. Scratch restored after.

README updated with the switch --certify usage.
