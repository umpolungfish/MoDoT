Give the agent on-the-fly ob3ect creation: TOOL: ob3ect <description>

The agent could imscribe a missing catalog entry but had no way to type a
procedure or entity through the real Ob3ect Auto-Designer. Now `TOOL:
ob3ect <free-text description>` shells to ~/ob3ect/auto.py (its own venv,
cwd ~/ob3ect, --no-diagram --no-scaffold, retries bounded at 3 so the
pipeline's retry-forever default cannot hang a round) and returns the full
report: opcodes, Frobenius split/fuse verdict, registers, bootstrap
sequence, topology. The artifact persists to ~/ob3ect/digital/<slug>/ like
every ob3ect, so it is durable, not a throwaway.

The system prompt tells the agent to describe the entity neutrally (what it
is and does, naming no candidates) and to use the verb to ground a protocol
or structure it is about to rely on. Registered in STRUCTURAL_VERBS and
verb_usage so misses get actionable feedback.

Verified live with the exact command the runner issues: a split/recombine
description came back mu o delta = id PASS, Valid: True, saved to digital/.
Builds clean, all 21 tests pass.
