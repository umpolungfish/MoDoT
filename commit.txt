Transcript review: the evening perfect-cuboid runs, mined from tool_calls

One unhealthy run and one healthy sibling. The unhealthy one re-proved the
same admitted tool 86 times, once per round for 17 minutes, and fired 34
calls with a literal ellipsis placeholder as an argument; both point at the
round prompt, not the model. The kernel gate behaved correctly throughout:
two ill-typed defines refused, the well-typed cuboid_descent admitted.
Blind spot recorded: the ob3ect auto-designer logs no tool calls.
