The Ħ door as a tool: imasm arev <opcode word> — the agent holds a word up to both hands.

mOMonadOS built the door (Kernel::arev_hop: the R1/R2 evidence triples exchanged
over one shared substrate, hop∘hop = id exact, O_inf_dag through the mirror = O_∞).
This gives it to the MoDoT agent as a native tool.

ask_native/src/arev.rs — a faithful port of the kernel's self-imscription, tick
accumulator semantics (EVALT/EVALF B-live and gate counts, ENGAGR pushes B,
FSPLIT/FFUSE fork frames with resume jump, TANCH root halt, natural-wrap
winding), value-period ring, compute_tier, and the Ħ mirror. The tool statically
imscribes the word, winds it 16 wraps for the dynamic evidence, then reads the
tier from the or' hand and the flipped hand, verifying hop∘hop = id on every
call. Dispatched as `imasm arev|hop|door <word>`; TOOL line added to the brief.

First readouts, spoken in advance by the kernel experiment and confirmed here:
- IMSCRIB ◇ ● IMSCRIB (the replicative opening) — or' O_inf_dag, flipped O_∞:
  the lateral partner at the same shell, exactly as in QEMU.
- VINIT ⊙ ◇ + ⊞ × ● VINIT (dialetheic closure) — both hands O_∞: ACHIRAL.
  A word whose evidence is symmetric reads the same through the mirror; the
  door also measures chirality, not just crosses it.

All 115 existing tests pass.
